# ZooKeeper Configuration

dataDir={{ zookeeper_data_dir }}
clientPort={{ zookeeper_client_port }}
maxClientCnxns={{ zookeeper_max_client_connections }}
tickTime={{ zookeeper_tick_time }}
initLimit={{ zookeeper_init_limit }}
syncLimit={{ zookeeper_sync_limit }}

# ZooKeeper cluster servers
{% for server in zookeeper_servers %}
server.{{ loop.index }}={{ server.hostname }}:{{ server.leader_port }}:{{ server.election_port }}
{% endfor %}

# Whitelisted 4lw commands
4lw.commands.whitelist={{ zookeeper_whitelist_commands }}
